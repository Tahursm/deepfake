# Deep Learning Framework
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Computer Vision & Image Processing
# Note: opencv-python pinned to version compatible with numpy<2.0
opencv-python>=4.8.0,<4.12.0
mediapipe>=0.10.0,<0.11.0
Pillow>=10.0.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.11.0

# Data Processing
# Note: numpy<2.0 required for mediapipe compatibility. Some packages may show warnings
# about requiring numpy>=2.0, but they should still function with numpy 1.x
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Visualization & Explainability
matplotlib>=3.7.0
seaborn>=0.12.0
grad-cam>=1.4.0

# Web Framework & Deployment
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
jinja2>=3.1.0
aiofiles>=23.0.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
tensorboard>=2.13.0
wandb>=0.15.0

# Video Processing
ffmpeg-python>=0.2.0

# Protobuf version constraint (pinned for mediapipe compatibility)
# mediapipe 0.10.21 requires protobuf<5,>=4.25.3
# Note: Some packages (ydf, opentelemetry-proto, grpcio-status) may show warnings
# about requiring protobuf>=5.0, but they should still function with protobuf 4.x
# If you need mysql-connector-python, you may need to uninstall it first:
# pip uninstall mysql-connector-python
protobuf>=4.25.3,<5.0.0

# Optional: For better performance
# Install CUDA-enabled PyTorch if GPU available
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

